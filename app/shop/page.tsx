'use client';
import { useEffect, useState } from 'react';
import { Header } from '@/components/header';
import { ProductCard } from '@/components/product-card';
import type { Product, Category } from '@/lib/types';
import { useSearchParams } from 'next/navigation';

export default function Shop(){const search=useSearchParams();const [products,setProducts]=useState<Product[]>([]);const [cats,setCats]=useState<Category[]>([]);const [active,setActive]=useState(search.get('category')||'all');useEffect(()=>{fetch('/api/products').then(r=>r.json()).then(d=>setProducts(d.products||[]));fetch('/api/categories').then(r=>r.json()).then(d=>setCats((d.categories||[]).filter((c:Category)=>!c.hide)));},[]);const filtered=active==='all'?products:products.filter(p=>typeof p.category==='object'?p.category?.name.toLowerCase()===active.toLowerCase():cats.find(c=>c.id===p.category)?.name.toLowerCase()===active.toLowerCase());return <main><Header/><div className="shop-page"><span className="eyebrow">DE MARKTPLAATS VAN DE SMP</span><h1 className="shop-title">De <em>Werkplaats</em></h1><p style={{color:'#927e66',maxWidth:650}}>Ontdek alle machines, upgrades en exclusieve items. Elk product wordt rechtstreeks uit Tip4Serv geladen.</p><div className="filters"><button className={`filter ${active==='all'?'active':''}`} onClick={()=>setActive('all')}>ALLES</button>{cats.map(c=><button key={c.id} className={`filter ${active===c.name.toLowerCase()?'active':''}`} onClick={()=>setActive(c.name.toLowerCase())}>{c.name.toUpperCase()}</button>)}</div><div className="products">{filtered.map(p=><ProductCard key={p.id} product={p}/>)}</div>{!filtered.length&&<div className="empty">Geen producten gevonden. Controleer je Tip4Serv-configuratie.</div>}</div></main>}